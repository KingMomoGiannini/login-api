/*
 * Configuración de seguridad anterior usando JWT directo.
 * Reemplazada por OAuth2ResourceServerConfig y OAuth2AuthorizationServerConfig
 * para usar OAuth2 en lugar de JWT directo.
 * 
 * Este archivo se mantiene como referencia pero está deshabilitado.
 */
